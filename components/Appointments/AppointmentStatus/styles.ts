import { styled } from '@mui/system';

export const styles = {};

export const AppointmentStatusStyled = styled('span')({
    padding: '6px 12px',
    marginRight: '5px',
    borderRadius: 16,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '14px',
    lineHeight: '18px',
    color: 'white',
});
