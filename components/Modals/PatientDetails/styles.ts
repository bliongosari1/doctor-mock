import { styled } from '@mui/system';

export const ContentWrapper = styled('div')({
    height: '80vh',
    width: '47vw',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
});

export const styles = {};
